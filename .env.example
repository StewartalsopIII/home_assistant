# Copy to `.env` and fill in real values.
#
# Vapi (server-side). Use a *Private API Key* from dashboard.vapi.ai (not the Public API Key).
VAPI_PRIVATE_API_KEY=
VAPI_ASSISTANT_ID=

# Vapi public key (client SDK only; not used by the Pi bridge)
VAPI_PUBLIC_API_KEY=

# (Optional) Home Assistant — only needed once we add tool calling.
HA_URL=http://localhost:8123
HA_TOKEN=
